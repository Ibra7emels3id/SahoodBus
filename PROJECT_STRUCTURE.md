# بنية مشروع حافلة سهود

تم تقسيم المشروع إلى ثلاثة أجزاء مستقلة وظيفياً حتى تظل تعديلات الواجهة، منطق الأعمال، وعقود البيانات واضحة ومنفصلة.

| المجلد | المسؤولية | أمثلة على التعديلات المستقبلية |
|---|---|---|
| `client/` | **الواجهة الأمامية React**: الصفحات، مكونات لوحة التحكم، التصميم، التنقل، وحالة Zustand. | إضافة صفحة، تعديل شاشة الحجوزات، تغيير الألوان أو تجربة المستخدم. |
| `server/` | **الواجهة الخلفية Express وtRPC**: إجراءات API، الصلاحيات، التحقق، وقواعد الأعمال. | إضافة عملية حفظ حجز، ضبط صلاحيات الفرع، أو إنشاء تقرير جديد. |
| `shared/` | **العقود والمنطق المشترك**: مخططات Zod وأنواع البيانات والحسابات القابلة للاختبار. | إضافة حقل للحجز أو الرحلة مع ضمان تطابق الواجهة والخلفية. |
| `drizzle/` | **نموذج قاعدة البيانات والترحيلات الحالية**. | تحديث المخطط عند العمل على قاعدة البيانات المتاحة في المشروع. |
| `docs/` | **توثيق واجهات البرمجة والقرارات التقنية**. | توثيق إجراءات أو تكاملات جديدة مثل MongoDB. |

> لا تستدعي الواجهة قاعدة البيانات مباشرة. تمر كل عملية تشغيلية عبر `server/`، وتستعمل الواجهة عقود `shared/` لضمان أن المدخلات متوافقة مع تحقق الخادم.

عند ربط MongoDB، يبقى هذا الفصل نفسه كما هو: توضع طبقة الاتصال والاستعلامات داخل `server/`، بينما تظل صفحات React وZustand داخل `client/` دون معرفة تفاصيل قاعدة البيانات.
